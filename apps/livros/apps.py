from django.apps import AppConfig


class LivrosConfig(AppConfig): 
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.livros'   # colocamos o caminho do disco rigido apps/animal
